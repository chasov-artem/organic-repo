import styles from "./NewCollection.module.scss";

const NewCollection = () => {
  return (
    <section className={styles["new-collection"]}>
      {/* Контент NewCollection */}
    </section>
  );
};

export default NewCollection;
